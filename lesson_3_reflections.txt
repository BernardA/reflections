When would you want to create a remote repository rather than keeping all your work locally?
Mostly to work with other people, but could also be useful when working from multiple locations

Why might you want to always pull changes manually rather than having Git automatically stay up-to-date with your remote repository?
To have more logical steps incorporated into each commit

Describe the differences between forks, clones, and branches.  When would you
use one instead of another?

    Forks is a clone on github computers. Clone is a copy from github to local and branch is a way to separate the code when attempting new features while preserving the main branch, or master. 

What is the benefit of having a copy of the last known state of the remote
stored locally?

    easy comparison offline

How would you collaborate without using Git or GitHub?  What would be easier,
and what would be harder?

    harder

When would you want to make changes in a separate branch rather than directly in
master?  What benefits does each approach have?

    Fill in your answer here

git pull origin master ( pull latest version from remote )
git pull = git fetch + git merge